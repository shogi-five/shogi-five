package model.operator;

import model.board.Board;
import model.piece.Piece;
import model.selection.Selection;

public class Operator {
    public Board movePiece(Board board, Piece piece, int position) {
        // 駒を移動させ、次の盤面を返す処理を実装する
        // ...
        return newBoard;
    }

    public Selection[] getAvailableMoves(Board board, Piece[] pieces) {
        // 移動可能な選択肢のリストを返す処理を実装する
        // ...
        return availableMoves;
    }

    // 以下に駒の移動や盤面の更新などのメソッドを追加してください
    // ...
}
